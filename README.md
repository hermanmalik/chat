# chat in terminal

This is so I can say "chat what's the capital of Namibia" like a Twitch streamer. Useful for quick questions, espcially syntax and translations.

You will want to alias `chat` to `chat.py` in your shell configuration and make the script executable. 